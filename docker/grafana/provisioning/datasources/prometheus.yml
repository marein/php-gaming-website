apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    isDefault: true
    access: proxy
    uid: prometheus
    url: $GF_DATASOURCE_PROMETHEUS_URL
    jsonData:
      timeInterval: 5s
    version: 1
    editable: true
