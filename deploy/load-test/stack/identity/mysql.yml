version: '3.4'

services:
    identity-mysql:
        image: marein/php-gaming-website:mysql
        command: mysqld --max-connections=1000
        environment:
            MYSQL_ROOT_PASSWORD: password
        volumes:
            - identity-mysql:/var/lib/mysql
        deploy:
            placement:
                constraints:
                    - "node.labels.identity-mysql==1"
    identity-mysql-exporter:
        image: prom/mysqld-exporter
        environment:
            DATA_SOURCE_NAME: "root:password@(identity-mysql:3306)/"
        deploy:
            placement:
                constraints:
                    - "node.labels.identity-mysql==1"
            labels:
                - "prometheus-job=mysql"
                - "prometheus-port=9104"

volumes:
    identity-mysql:
